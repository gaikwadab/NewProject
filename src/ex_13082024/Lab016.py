# Take the user input and calculate the sum, mul, Div

# When we ask any for any input from user it is by default in String and that's the reason we have to
# convert them into the integer and to convert them into the integer we have to
# follow below format we have to pass them into int function


A = int(input('Enter the first number'))
B = int(input('Enter the second Number'))

Mul = print('multiplication of above number is ',(A*B))
Sum = print('sum of baove two numbers is ',(A+B))
Div = print('Div of above two numbers is ',(A/B))

